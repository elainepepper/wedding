rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All guest and manager data is accessed through authenticated server API
    // routes. Browser-side Firestore access is deliberately disabled.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
