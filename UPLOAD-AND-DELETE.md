# This pack: one CSS fix

`app/globals.css` — fixes the Menu, Next, Back and Sound buttons piling up
in the top-left corner instead of sitting in their own corners.

## Upload
GitHub → **Add file → Upload files** → drag in the `app` folder (or just
`globals.css` itself, into `app/`) → commit.

## Still need deleting (from before)
- `app/editor-preview/` (the whole folder) — **breaks the build if left**
- `app/manager/WebsiteEditor.tsx`
- `app/welcome/WelcomeExperience.tsx`
- `public/wedding/hero-portrait-small.webp`

Open each on GitHub → **⋯** → **Delete file** → commit.
