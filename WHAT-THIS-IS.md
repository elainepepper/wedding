# This pack: one file

`app/manager/manager.css`

On a phone, the invitation links were a four-column table, so the buttons —
the only part you press — were pushed off the right of the screen. Each
household is now a card: name, who it is for, the link, then four full-width
buttons with **Copy message** first, since that is the one you use most.

Desktop is unchanged.

GitHub → **Add file → Upload files** → drag in the `app` folder → commit.
